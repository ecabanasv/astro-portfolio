---
import { stats } from "../data/stats";
---
<section id="impact" class="section container">
  <div class="flex flex-wrap items-center justify-between gap-3">
    <div>
      <p class="text-xs font-semibold uppercase tracking-[0.2em] text-blue-700 dark:text-emerald-200">Impact</p>
      <h2 class="mt-2 text-2xl md:text-3xl font-semibold">What a themed demo unlocks</h2>
    </div>
    <p class="text-sm text-gray-600 dark:text-gray-300 max-w-md">Drop these metrics into your template or adjust copy per engagement—each card is editable.</p>
  </div>

  <div class="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">
    {stats.map(item => (
      <article class="card">
        <p class="text-sm font-semibold text-blue-700 dark:text-emerald-200">{item.label}</p>
        <p class="mt-3 text-3xl font-semibold text-gray-900 dark:text-white">{item.value}</p>
        <p class="mt-2 text-sm text-gray-600 dark:text-gray-300">{item.hint}</p>
      </article>
    ))}
  </div>
</section>
